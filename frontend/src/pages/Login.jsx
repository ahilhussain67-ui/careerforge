import {useState} from "react";import {loginUser} from "../services/api";
export default function Login({onLogin,onSwitchRegister}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setError("");setLoading(true);try{onLogin(await loginUser(email,password))}catch(err){setError(err.message)}finally{setLoading(false)}};return <form className="auth-card" onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h2>Sign in to CareerForge</h2><p>Continue building the career you want.</p>{error&&<div className="form-error">{error}</div>}<label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required type="password" minLength="6" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label><button className="button" disabled={loading}>{loading?"Signing in…":"Sign in"}</button><div className="auth-switch">New to CareerForge? <button type="button" className="text-button" onClick={onSwitchRegister}>Create an account</button></div></form>}
