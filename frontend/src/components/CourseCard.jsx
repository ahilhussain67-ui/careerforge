import ProgressBar from "./ProgressBar";
export default function CourseCard({course,onOpen,progress={}}){const total=course.lessons?.length||0,completed=progress[course.id]?.completedCount||0,value=total?Math.round(completed/total*100):0;return <article className="course-card"><div className="card-top"><span className="pill">{course.category}</span><span className="card-level">{course.difficulty||"All levels"}</span></div><div className="course-card-title"><h3>{course.name}</h3><span className="course-arrow">↗</span></div><p>{course.description}</p><div className="course-meta"><span><b>{total}</b> lessons</span><span><b>{course.estimatedDuration||"Self paced"}</b></span></div><div className="progress-label"><span>Course progress</span><strong>{value}%</strong></div><ProgressBar value={value}/><div className="course-progress-copy"><span>{completed} of {total} complete</span><span>{value === 100 ? "Completed" : completed ? "In progress" : "Not started"}</span></div><button className="button" onClick={()=>onOpen(course)}>{completed?"Continue learning":"Start learning"}</button></article>}
