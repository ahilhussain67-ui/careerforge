export default function JobCard({ job, onView }) { return <article className="job-card"><div className="job-main"><div className="job-card-top"><span className="company-avatar">{job.company?.charAt(0)}</span><div><strong>{job.company}</strong><p>{job.location}</p></div></div><h3>{job.title}</h3><div className="job-meta"><span>{job.jobType}</span><span>{job.experienceLevel}</span>{job.remoteAvailable&&<span className="remote-badge">Remote</span>}{job.postedDate&&<span>Posted {new Date(job.postedDate).toLocaleDateString()}</span>}</div><div className="skills-list">{job.skills?.split(", ").map(s => <span key={s}>{s}</span>)}</div></div><div className="job-actions"><b>{job.salaryRange}</b><button className="button secondary" onClick={() => onView(job)}>View role <span aria-hidden="true">↗</span></button></div></article>; }
